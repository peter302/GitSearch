import { GitthroughDirective } from './gitthrough.directive';

describe('GitthroughDirective', () => {
  it('should create an instance', () => {
    const directive = new GitthroughDirective();
    expect(directive).toBeTruthy();
  });
});
